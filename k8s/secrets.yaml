# Secret - Variables de entorno SENSIBLES (contraseñas, tokens)
# IMPORTANTE: En producción, estos valores deben estar en base64 y nunca commitearse al repo
apiVersion: v1
kind: Secret
metadata:
  name: explora-secrets
  namespace: explora
type: Opaque
stringData:
  # Credenciales de PostgreSQL
  POSTGRES_PASSWORD: "explora123"
  
  # JWT Secret - CAMBIAR EN PRODUCCIÓN
  JWT_SECRET: "tu_secreto_jwt_muy_largo_y_aleatorio_cambialo_en_produccion"
  
  # Credenciales de admin por defecto - CAMBIAR EN PRODUCCIÓN
  ADMIN_USERNAME: "admin"
  ADMIN_PASSWORD: "admin123"
